<%= form_for(@envio) do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class="field">
		<%= f.text_area :status %> 
	</div>
	<div class="actions">
		<%=f.submit "Submit" %>
	</div>
<% end %>